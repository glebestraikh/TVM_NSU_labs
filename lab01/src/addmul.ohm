AddMul {
  AddExpr = AddExpr "+" MulExpr  -- add
          | MulExpr

  MulExpr = MulExpr "*" PrimExpr  -- mul
          | PrimExpr

  PrimExpr = "(" AddExpr ")"  -- paren
           | number

  number = digit+
}